\chapter{Einleitung}
\label{ch:Einleitung}

%
% Was ist ein APT-Scanner?
%
Ein Advanced-Persistent-Threat-Scanner (APT-Scanner), oder präziser ein APT-Angriffserkennungssystem,
ist eine spezialisierte Sicherheitssoftware,
die darauf ausgelegt ist,
die Aktivitäten von fortgeschrittenen andauernden Bedrohungen zu identifizieren \cite[S.~1--2]{Buchta:2024}.

\noindent Während herkömmliche Virenscanner oft auf bekannten Signaturen basieren,
müssen APT-Scanner deutlich komplexere Methoden anwenden,
um die typischerweise subtilen
und über lange Zeiträume gestreckten Angriffe zu finden.

\vspace{7.56mm}
\noindent Der Begriff APT steht für Advanced Persistent Threat
und bezeichnet eine Form des Cyberangriffs,
bei der sich ein Angreifer unbefugten Zugang zu einem Computersystem verschafft
und potenziell über einen langen Zeitraum unentdeckt darin verbleibt.
Im Gegensatz zu herkömmlichen Angriffen
handelt es sich bei APTs um menschliche Akteure
mit hoher Motivation und hohen Ressourcen,
nicht um einfachen, automatisierten Schadcode \cite[S.~1--2, 7]{Buchta:2024}.

\vspace{7.56mm}

%
% Anomalieerkennung
%
\begin{sloppypar}
\noindent Da APT-Akteure häufig neue Schwachstellen (Zero-Day-Exploits) nutzen
oder legitime Systemwerkzeuge missbrauchen (Living-off-the-Land),
helfen einfache Blacklists oft nicht weiter \cite[S.~6--7]{Buchta:2024}.
Moderne APT-Angriffs\-erkennungs\-systeme nutzen daher Anomalieerkennung \cite[S.~8--9]{Buchta:2024}.
Die Anomalieerkennung identifiziert ungewöhnliche Muster
oder Instanzen in Daten,
die nicht dem erwarteten Verhalten entsprechen \cite[S.~8--9, 24]{Buchta:2024}.
Sie unterscheidet im Gegensatz zu herkömmlichen Virenscannern
nicht zwischen bekannten und unbekannten Angriffen.
Da die Anomalieerkennung auf dem Systemverhalten basiert,
um Abweichungen zu erkennen,
sind APT-Angriffs\-erkennungs\-systeme
mit einem hohen Wartungsaufwand verbunden.
Das APT-Angriffs\-erkennungs\-system lernt zunächst
das „normale" Verhalten des Rechners.
Jede signifikante Abweichung wird als potenzieller Angriff gemeldet.
Analysten bewerten die potenziellen Angriffe
und erkennen False Positives,
welche ab diesem Zeitpunkt automatisch
von dem APT-Angriffs\-erkennungs\-system als solche eingeordnet werden.
\end{sloppypar}

\vspace{7.56mm}

%
% Performance-Relevanz und Ziel der Arbeit
%
\begin{sloppypar}
\noindent APT-Angriffs\-erkennungs\-systeme werden auf einer hohen Anzahl
von produktiven Systemen,
häufig auf kritischen Servern
oder in ressourcen\-beschränkten Umgebungen, eingesetzt.
Hierbei werden regelmäßig Scans ausgeführt.
Auch bei Incident-Response-Szenarien
kommen APT-Angriffs\-erkennungs\-systeme zum Einsatz.
Die Performance des APT-Angriffs\-erkennungs\-systems
ist von entscheidender Bedeutung.
Lange Scan-Zeiten und hohe Ressourcenauslastung
stellen ein relevantes Problem dar.
Das Risiko von Performanceeinbußen während des Scans
auf produktiven Systemen muss möglichst gering gehalten werden.
Gleichzeitig muss ein Scan auf einem potenziell befallenen System
eine niedrige Scan-Zeit aufweisen
und die zur Verfügung stehenden Ressourcen
möglichst weitreichend nutzen.
Um diese Voraussetzungen an die Performance zu erfüllen,
ist eine effiziente Parallelisierung durch einen Scheduler
von hoher Relevanz.

\vspace{7.56mm}

\noindent In dieser Arbeit wird der Versuch vorgenommen,
die Performance des APT-Scanners Thor
der Firma Nextron Systems GmbH,
im Folgenden als Scanner bezeichnet, zu optimieren,
indem ein task-basierter Scheduler implementiert wird.
\end{sloppypar}

\section{Grundlagen}
\begin{sloppypar}
In diesem Kapitel wird beschrieben, was Performance für Anwendungszweck des Scanners bedeutet.
Danach wird auf die Arbeitsweise des Scanners eingegangen, insofern es für das Verständnis notwendig ist.

\noindent Es wird auf den aktuellen Stand der Forschung im Bezug auf Scheduling Algorithmen eingegangen.
Zuletzt wird ein kurzer Überblick über den Aufbau der Arbeit gegeben.

\subsection{Performance}
Wichtige Metriken für die Bewertung von CPU-Scheduling-Algorithmen sind \cite[S.~204--205]{Silberschatz:2018}, \cite[S.~154]{Tanenbaum:2015}:
\begin{itemize}
  \item \textbf{CPU Utilization:} Das Ziel ist es, den Prozessor so beschäftigt wie möglich zu halten. In realen Systemen sollte die Auslastung zwischen 40 \% (leicht belastet) und 90 \% (stark belastet) liegen.
  \item \textbf{Throughput:} Dies ist die Anzahl der Prozesse, die pro Zeiteinheit abgeschlossen werden.
  \item \textbf{Turnaround Time:} Dies beschreibt die Zeitspanne von der Einreichung eines Prozesses bis zu seinem Abschluss.
  \item \textbf{Waiting Time:} Die Zeitspanne, die ein Prozess in der Warteschlange verbringt.
\end{itemize}

\vspace{7.56mm}

\noindent Wenn man den gesamten Prozess betrachtet, treten andere Metriken in den Vordergrund:
\begin{itemize}
  \item \textbf{RAM Utilization:} Für Ressourcen, die Anfragen bedienen, ist die Auslastung ein Maß dafür,
   wie beschäftigt eine Ressource ist, basierend darauf, wie viel Zeit sie in einem gegebenen Intervall aktiv Arbeit verrichtet hat.
   Für Ressourcen, die Speicher bereitstellen, kann sich die Auslastung auf die verbrauchte Kapazität, in diesem Fall den Hauptspeicher, 
   beziehen \cite[S.~22]{Gregg:2020}.
  \item \textbf{Runtime:} Hennessy und Patterson \cite[S.~39--40]{Hennessy:2019} schreiben, dass die Ausführungszeit als das einzig konsistente und zuverlässige Maß für die Leistung gilt. 
\end{itemize}

\noindent Die Definition der Performance ist stark von dem zu bewertenden System abhängig.
In dieser Arbeit wird die Performance des Scanners nach dessen Leistung in den aufgezählten Metriken bewertet. 
\end{sloppypar}

\subsection{Bisherige Arbeitsweise des Scanners}
\begin{sloppypar}
Es gibt in dem Scanner zwei Arten von Komponenten, die für das Verständnis des Arbeitsablauf relevant sind. \marginpar{Die Beschreibung der Arbeitsabläufe des Scanners in diesem Abschnitt sind stark vereinfacht und auf den für das Verständnis dieser Arbeit relevanten Teil heruntergebrochen.}
\begin{itemize}
  \item \textbf{Modul:} Module sind alleinstehende Aufgaben, welche eine nach der anderen ausgeführt werden.
  Zum Beispiel durchsucht ein Modul namens "User Account Check" das System nach allen Benutzerkonten und erstellt aus diesen jeweils ein Objekt.
  Das Modul lässt die Objekte daraufhin untersuchen \cite{thor:modules}.
  \item \textbf{Feature:} Features werden aktiviert, wenn bestimmte Objekte untersucht werden und arbeiten mit diesen Objekten.
  Welche Features auf einem Objekt aktiviert werden ist vom Typ des Objekts abhängig.
  Features können dabei weitere Objekte generieren.
  Zum Beispiel findet das "File System Scan" Modul während des Scans eine .zip Datei und generiert ein Objekt daraus. 
  Bei der Untersuchung dieses Objekts wird das "Archive Scan" Feature aktiviert, entpackt die .zip Datei und generiert pro darin enthaltener Datei ein weiteres Objekt.
  Diese Objekte werden wiederum untersucht und je nach Objektyp werden Features auf ihnen aktiviert \cite{thor:features}. 
\end{itemize}


\noindent Der Scanner arbeitet mit objektbasierten synchronen Multithreading Scheduling.
Jedes Modul startet während der Ausführung seinen eigenen primitiven Scheduler, inklusive eigener Warteschlange und eigenen Threadpool.
Das Modul untersucht das System und generiert dabei Objekte, welche es in die Warteschlange einreiht.
Die Worker aus dem Threadpool nehmen sich die Objekte und führen darauf die Features, welche für den Objekttyp registriert sind, aus.

\vspace{7.56mm}

\noindent Bei der Ausführung jedes Features können viele weitere Objekte, im folgenden Kinder-Objekte genannt, entstehen.
Auch auf diesen Kinder-Objekten werden je nach Objekt-Typ wieder eine Anzahl an Features ausgeführt, bei welchen wiederum weitere Objekte entstehen können.
Das Objekt, sowie alle Kinder-Objekte und auch deren Nachkommen werden von dem Worker synchron untersucht.
So kann es zu Szenarien kommen, in denen ein Worker lange synchron arbeitet und dabei potenziell eine große Menge Hauptspeicher benötigt.


\noindent Abbildung \ref{fig:scanner-workflow} zeigt den beschriebenen Arbeitsablauf des Scanners.

\begin{figure}[htbp]
\centering
\resizebox{\textwidth}{!}{\begin{tikzpicture}[
    >=Latex,
    node distance=10mm and 18mm,
    every node/.style={font=\sffamily\small},
    startstop/.style={ellipse, draw, thick, minimum width=28mm, minimum height=10mm, align=center},
    process/.style={rectangle, draw, thick, rounded corners=2pt, minimum width=38mm, minimum height=10mm, align=center},
    decision/.style={diamond, draw, thick, aspect=2, align=center, inner sep=1pt},
    datastore/.style={cylinder, shape border rotate=90, draw, thick, aspect=0.25, minimum height=14mm, minimum width=12mm, align=center},
    flow/.style={->, thick},
    groupbox/.style={draw, thick, rounded corners=4pt, inner sep=10pt},
    workerbox/.style={draw, thick, rounded corners=4pt, inner sep=10pt, fill=gray!5},
    recursivebox/.style={draw=red!70!black, thick, dashed, rounded corners=4pt, inner sep=10pt, fill=red!5}
]

% --- Global Start ---
\node[startstop] (start) {Start Scanner};
\node[datastore, below=of start] (modlist) {Modul-\\liste};
\node[decision, below=of modlist] (hasnext) {Weiteres\\Modul?};
\node[process, below=of hasnext] (load) {Modul laden};
\node[startstop, right=25mm of hasnext] (end) {Ende};

\draw[flow] (start) -- (modlist);
\draw[flow] (modlist) -- (hasnext);
\draw[flow] (hasnext) -- node[right]{JA} (load);
\draw[flow] (hasnext) -- node[above]{NEIN} (end);

% --- Module Context ---
\node[process, below=of load] (init) {Initialisiere Scheduler, Warteschlange \& Threadpool};
\node[process, below=of init] (scan) {Systemscan: Initial-Objekte finden};
\node[datastore, below=of scan] (queue) {Modul-\\Warteschlange};

\draw[flow] (load) -- (init);
\draw[flow] (init) -- (scan);
\draw[flow] (scan) -- node[right]{Einreihen} (queue);

% Module group box
\begin{pgfonlayer}{background}
\node[workerbox, fit=(init)(scan)(queue), label={[yshift=-6pt]right:\textbf{Modul-Ebene}}] (modulebox) {};
\end{pgfonlayer}

% --- Worker Loop ---
\node[process, below=20mm of queue] (process) {Verarbeite Objekt};
\node[decision, below=of process] (select) {Features für\\Objekttyp?};
\node[process, below=of select] (run) {Feature ausführen};
\node[decision, below=of run] (newobj) {Neues Kind-\\Objekt?};
\node[process, below=of newobj] (finish) {Objekt abgeschlossen};

% Connections
\draw[flow] (queue) -- node[right]{Worker entnimmt Objekt} (process);
\draw[flow] (process) -- (select);
\draw[flow] (select) -- (run);
\draw[flow] (run) -- (newobj);

% Recursive YES path
\draw[flow] (newobj.east) -- ++(25mm,0) |- node[right, pos=0.25, align=left] {JA: Unmittelbare \\ Weiterverarbeitung} (process.east);


% NO path
\draw[flow] (newobj) -- node[right]{NEIN} (finish);

% Back to Queue
\draw[flow] (finish.west) -- ++(-25mm,0) |- node[left,pos=0.25, align=left]{Nächstes Objekt \\ aus Queue} (queue.west);

% Back to module list after module is done
\draw[flow] (modulebox.west) -- ++(-10mm,0) |- node[left,pos=0.15, align=left]{Modul \\ abgeschlossen} (hasnext.west);

% Worker group box
\begin{pgfonlayer}{background}
\node[workerbox, fit=(process)(select)(run)(newobj)(finish), label={below:\textbf{Synchroner Worker-Durchlauf}}] {};
\end{pgfonlayer}

% Recursive sub-box
\begin{pgfonlayer}{background}
\node[recursivebox, fit=(run)(newobj), label={[anchor=south,yshift=4pt]}] {};
\end{pgfonlayer}

\end{tikzpicture}}
\caption{Arbeitsablauf des Scanners}
\label{fig:scanner-workflow}
\end{figure}



\subsection{Stand der Wissenschaft: Scheduler}

\subsection{Aufbau der Arbeit}
\end{sloppypar}
